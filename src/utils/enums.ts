export const enum MinMaxFunction {
  Min = "min",
  Max = "max",
}
export const enum EqualitySign {
  Equal = "=",
  GreaterThanOrEqual = ">=",
  LessThanOrEqual = "<=",
}

export const enum VarType {
  Slack = "s",
  Artificial = "a",
  Regular = "x",
}
